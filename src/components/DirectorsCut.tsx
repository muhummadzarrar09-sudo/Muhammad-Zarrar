"use client";

/**
 * DirectorsCut component is now deprecated as a toggle.
 * Cinematic mode (Director's Cut) is ALWAYS ON by default for the portfolio.
 * This file is kept for any future internal hooks if needed.
 * All effects are now permanently enabled in App + CSS + components.
 */
export function DirectorsCut() {
  return null;
}
