/**
 * Removed globally per art direction: the site no longer renders the
 * section pill/placard labels.
 */
export function Placard(_: {
  no: string;
  title: string;
  medium: string;
  tone?: "light" | "dark";
}) {
  return null;
}
